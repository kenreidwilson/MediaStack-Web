import { Theme } from '../types';

export const LightTheme: Theme = { 
    name: 'light',
    style: {
        backgroundColor: 'white',
        color: 'black',
        primaryColor: 'rgb(0, 102, 255)'
    }
}
